/**
 * Frontend scripts for Card to Card payment gateway.
 */
jQuery(document).ready(function($) {
    // بازخورد لحظه‌ای درگ اند دراپ در صورت فعال تکی
    var p2pForm = $('#p2pStandalonePaymentForm');
    if (p2pForm.length) {
        // فلوهای تکمیلی
    }
});
